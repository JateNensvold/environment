# Creviewcommit

Read `~/.agents/workflows/creviewcommit.md` and use it as the source of truth for this
workflow.
Run each stage in order, returning to analysis between stages, and stop after local commit
preparation.
