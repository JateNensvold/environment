---
name: cprep
description: Calls $creview, $ctest, $cdocument, and $ccommit. Run the local pre-submit
  pipeline without pushing.
---

# Cprep

Use `~/.agents/workflows/cprep.md` as the source of truth for this workflow.
Read it first, then run each stage in order, returning to analysis between stages.
