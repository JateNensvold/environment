# Git commits
- Never add `Co-Authored-By` lines to commit messages

# Session workflow
- Never resume sessions — always start fresh
- When a task is successfully committed (build passes, tests pass, pushed to GitHub), update this CLAUDE.md with any relevant context, decisions, or patterns learned during the session so the next fresh session has that information
